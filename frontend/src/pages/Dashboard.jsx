/**
 * pages/Dashboard.jsx — Meeting analysis results dashboard.
 *
 * Phase 4: Full implementation (summary, topics, action items,
 *          decisions, unresolved questions, charts via Recharts).
 * Phase 0: Stub — placeholder only.
 */

export default function Dashboard() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] gap-6 px-4">
      <h1 className="text-3xl font-bold text-indigo-400">Meeting Dashboard</h1>
      <p className="text-slate-400 max-w-md text-center">
        {/* Phase 4: replace with full dashboard layout */}
        Dashboard coming in Phase 4.
      </p>
    </div>
  )
}
