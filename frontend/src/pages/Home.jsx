/**
 * pages/Home.jsx — Landing / upload page.
 *
 * Phase 1: File upload UI (drag-and-drop, file picker, progress).
 * Phase 0: Stub — placeholder card only.
 */

export default function Home() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] gap-6 px-4">
      <h1 className="text-3xl font-bold text-indigo-400">Upload a Meeting Recording</h1>
      <p className="text-slate-400 max-w-md text-center">
        {/* Phase 1: replace with UploadCard component */}
        Upload UI coming in Phase 1.
      </p>
    </div>
  )
}
