/**
 * components/index.js — barrel export for all UI components.
 * Add component exports here as they are built in Phase 4.
 *
 * Example:
 *   export { default as UploadCard } from './UploadCard'
 *   export { default as Dashboard }  from './Dashboard'
 */
